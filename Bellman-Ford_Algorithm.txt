#include<stdio.h>

#define ROW  9
#define COLUMN 3

int min(int a , int b) {
    if(a < b)  {
        return a;
    }
    return b;
}

void bellmanford(int e[ROW][COLUMN] , int d[6]){
    int i = 0 , j = 0;
    for(i = 0 ; i < 6-1 ; i++) {
        for(j = 0; j < ROW;j++) {
            d[e[j][1]] = min(d[e[j][1]] , d[e[j][0]] + e[j][2]);
        }
    }

    //Checking the neagative edge cycle 
    for(j = 0; j < ROW;j++) {
        if(d[e[j][0]] + e[j][2] < d[e[j][1]] ){
            printf("Your graph contains negative edge cycle");
            return;
        }
    }
